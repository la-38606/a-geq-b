# Dune build artifacts
_build/
*.install
*.merlin

# OCaml / opam local switch
_opam/

# Copyrighted reference book (personal-use PDF; do not redistribute via the repo)
inequalities-a-mathematical-olympiad-approach.pdf

# Python cache from the corpus / sdp scripts
__pycache__/

# Python virtual environment for the numerical SDP solver (see sdp/README.md)
.venv/

# LaTeX build artifacts (the committed PDF is the output; .tex is the source)
doc/*.aux
doc/*.log
doc/*.out
doc/*.toc
doc/*.synctex.gz

# Lean build artifacts and fetched packages (Mathlib); keep lake-manifest.json
lean/.lake/

# Browser-test dependencies and outputs (see web/e2e)
web/e2e/node_modules/
web/e2e/test-results/
web/e2e/playwright-report/

# Recorded demo videos (kept out of history; see scripts/record-demo.sh)
/demo/
